#ifndef SECRETS_H
#define SECRETS_H

// =============================================================
// SECRETS — Configuration Template
// Copy this file to secrets.h and fill in your values.
// secrets.h is in .gitignore and will not be committed.
// =============================================================

// WiFi Credentials
const char* WIFI_SSID   = "YOUR_WIFI_SSID";
const char* WIFI_PASS   = "YOUR_WIFI_PASSWORD";

// Backend Configuration
const char* BACKEND_URL = "http://192.168.x.x:8000/ingest";  // Update to your backend IP
const char* API_KEY     = "your-api-secret-key-from-env";    // Generate: openssl rand -hex 32

// User ID for identifying data in backend
const char* USER_ID     = "user-uuid-or-identifier";

#endif
